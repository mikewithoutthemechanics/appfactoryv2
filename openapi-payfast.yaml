openapi: 3.0.0
info:
  title: AppFactory PayFast API (Skeleton)
  version: 0.1.0
paths:
  /payfast/checkout:
    get:
      summary: Create PayFast checkout URL (skeleton)
      responses:
        '200':
          description: URL string
